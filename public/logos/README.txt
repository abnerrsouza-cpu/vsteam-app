Coloque aqui o logo da VS TEAM como `logo.png`.

Tamanho recomendado: 400x160px (proporção 2.5:1) com fundo transparente.
Ele aparecerá automaticamente:
  - na tela de login
  - no topo do menu lateral (admin e cliente)

Formatos aceitos: PNG (recomendado) ou SVG (renomeie para logo.png por enquanto).

Sem o logo, o sistema mostra o texto "VS TEAM" estilizado como fallback.
